backend:
  name: git-gateway
  branch: main
  # For local development with GitHub, uncomment:
  # name: github
  # repo: your-username/your-repo-name
  # branch: main

media_folder: "assets/images"
public_folder: "/assets/images"

collections:
  - name: "blog"
    label: "Blog Posts"
    folder: "_posts"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string", required: true }
      - { label: "Publish Date", name: "date", widget: "datetime", required: true, format: "YYYY-MM-DD HH:mm" }
      - { label: "Thumbnail Image", name: "thumbnail", widget: "image", required: true }
      - { label: "Summary/Excerpt", name: "excerpt", widget: "textarea", required: true, hint: "This will appear on the home page preview" }
      - { label: "Main Content", name: "body", widget: "markdown", required: true }
      - {
          label: "Language",
          name: "language",
          widget: "select",
          required: true,
          options: [
            { label: "English", value: "en" },
            { label: "Sinhala", value: "si" }
          ],
          default: "en"
        }
      - { label: "Layout", name: "layout", widget: "hidden", default: "post" }